单例模式：内存中只有一个实例；
工厂模式：创建对象的一种模式，创建方法客户端不可知；
原型模式：根据已有对象创建副本，copy；
代理模式：功能增强，控制被代理对象的访问；
委派模式：委派类控制访问，内部调用不同逻辑实现功能；
策略模式：不同算法的封装，可相互替换；
模板模式：主流程确定，具体流程可已有不同实现方式；
适配器模式：满足已有功能，适配新需求，兼容；
装饰模式：给对象进行包装，不改变核心功能；
观察者模式：发布-订阅，被观察者变动后通知观察者；